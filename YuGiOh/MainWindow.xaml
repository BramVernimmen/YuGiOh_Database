<Window x:Class="YuGiOh.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:YuGiOh.ViewModel"
        xmlns:local="clr-namespace:YuGiOh"
        mc:Ignorable="d"
        Title="MainWindow" Height="1000" Width="600">
    <Window.DataContext>
        <vm:MainViewModel />
    </Window.DataContext>
    <Grid>
        <ListBox Background="LightCoral" ItemsSource="{Binding Cards}">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Height="240">
                        <Image  Margin="4" Source="{Binding DisplayImageUrl}"/>
                        <TextBlock Text="{Binding Name}" Margin="10,0" Foreground="Black" VerticalAlignment="Center" FontSize="21" TextWrapping="WrapWithOverflow" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
